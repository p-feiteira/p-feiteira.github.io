# Architecture

> Auto-generated by /map on 2026-02-21

## Overview

This is a modern Next.js web application utilizing the App Router architecture, customized with a specialized styling capability including Tailwind CSS, Framer Motion for animations, and Radix UI headless components. The application leverages Next-Intl for internationalization, placing primary application views under a localized base path. 

```
┌─────────────────────────────────────────┐
│           [Next.js App Router]          │
│          (/src/app/[locale])            │
├─────────────────────────────────────────┤
│         [Business Logic Layer]          │
│   (React Query / Localized Contexts)    │
├─────────────────────────────────────────┤
│            [Data Layer]                 │
│      (API Routes / Local Utils)         │
└─────────────────────────────────────────┘
```

## Components

### Locale Views
- **Purpose:** Primary application pages supporting multi-language.
- **Location:** `src/app/[locale]/`
- **Dependencies:** `next-intl`

### Sections
- **Purpose:** Reusable, page-level visual sections.
- **Location:** `src/app/[locale]/sections/`
- **Dependencies:** `framer-motion`, `lucide-react`, `@radix-ui/*`

### API Routes
- **Purpose:** Backend functionality, such as handling contact forms.
- **Location:** `src/app/api/`
- **Dependencies:** `nodemailer`

### Lib & Queries
- **Purpose:** Shared data-fetching queries and configurations.
- **Location:** `src/app/[locale]/sections/lib/`
- **Dependencies:** `@tanstack/react-query`, native `fetch`.

## Data Flow

1. **User Interaction:** User visits the localized page (`/[locale]`).
2. **Data Fetching:** Front-end components utilize `@tanstack/react-query` or native Next.js fetch (for Server Components) to grab server data or interact with internal APIs (e.g. `/api/contact`).
3. **Internal Endpoints:** API endpoints like the contact handler ingest payload, perform operations (e.g. send emails via nodemailer), and return state.

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| Next-Intl | Lib | Handle translations and localized routing |
| React Query | State | Data-fetching cache and synchronization |
| GitHub | API | Likely used to fetch repository or activity data via `github.ts` |
| Local API | Internal API | Handle form submissions / server-side logic |

## Technical Debt

- [ ] Empty `src/components/` directory: Reusable components appear to be currently placed in `sections` or scattered. Consistency should be reviewed.
- [ ] No testing framework identified (`jest`, `cypress`, `playwright`, etc).
- [ ] `lib` folder inside `sections` instead of standard `src/lib`.

## Conventions

**Naming:** PascalCase for React components, kebab-case/camelCase for modules.
**Structure:** Domain-driven structure grouped roughly by locale and page.
**Testing:** No established pattern.
